---
# sops + age encryption for docker stack secrets.
#
# The age recipient below is the PUBLIC key. The private key is the
# SOPS_AGE_KEY GitHub Actions secret (backed up in the password manager)
# and is provisioned to hosts at /docker/secrets/age.key by the playbooks.
#
# Encrypt:  sops --input-type dotenv --output-type dotenv -e plain.env > docker/<stack>/.env.sops
# Edit:     SOPS_AGE_KEY_FILE=<key> sops edit --input-type dotenv --output-type dotenv docker/<stack>/.env.sops
creation_rules:
  - age: age1p5auerjzvhvtjmfjnqf6hulfmwcjqdnu7j6wzy8uwl3g64780yxsqkvwsz
