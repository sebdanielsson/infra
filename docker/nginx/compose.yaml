---
services:
  nginx:
    container_name: nginx
    image: nginx:1.31.6-alpine
    networks:
      - nginx
    ports:
      - "8765:80"
    volumes:
      - /opt/election-map:/usr/share/nginx/html:ro
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
    labels:
      - docktail.service.enable=true
      - docktail.service.name=election-map
      - docktail.service.description=Election map static site
      - docktail.service.port=80
      - docktail.service.protocol=http
      - docktail.service.direct=true
      - docktail.service.network=nginx
      - docktail.service.service-port=443
      - docktail.service.service-protocol=https
    # Static site: if the document root stops serving an index, the service
    # is broken, so a strict 2xx check is the right signal here.
    healthcheck:
      test: ["CMD", "curl", "-fsS", "-o", "/dev/null", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

networks:
  nginx:
    name: nginx
