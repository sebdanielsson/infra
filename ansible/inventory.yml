---
all:
  children:
    proxmox_nodes:
      hosts:
        hogsmeade:
    virtual_machines:
      hosts:
        mediaserver:
    sbc:
      hosts:
        flightradar:
          ansible_user: sebbo
